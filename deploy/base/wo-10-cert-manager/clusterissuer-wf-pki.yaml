# WO-10 — Wells Fargo PKI ClusterIssuer (CA secret bootstrapped via ESO into cert-manager ns in real env).
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: wf-pki-issuer
spec:
  ca:
    secretName: wf-pki-bootstrap-ca
