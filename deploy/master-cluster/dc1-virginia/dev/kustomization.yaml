# WO-2 — GitOps overlay: DC1 Virginia / dev (one overlay per target cluster recommended).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base
  - cluster-readiness-job.yaml
  - cfk-kafka-stub.yaml
